apiVersion: v1
kind: Secret
metadata:
  name: fproject-backend-secret
  namespace: default
type: Opaque
stringData:
  # stringData를 사용하면 자동으로 base64 인코딩됨
  # ArgoCD 사용 시 실제 값은 Sealed Secrets 또는 External Secrets로 관리 권장
  DB_PASSWORD: "PLACEHOLDER"
  GOOGLE_CLIENT_SECRET: "PLACEHOLDER"
